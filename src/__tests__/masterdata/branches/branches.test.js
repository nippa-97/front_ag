import { shallow } from 'enzyme';
import Branches from '../../../components/masterdata/branches/branches';

it("renders Branches without crashing", () => {
    shallow(<Branches />);
});

describe("signin process without errors", () => {
    it("validate username field works without errors", () => {
        
    });

   
});