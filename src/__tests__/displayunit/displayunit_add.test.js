import { shallow } from 'enzyme';
import DisplayUnits from '../../components/displayUnits/DisplayUnits';

it("display units add renders without crashing", () => {
    shallow(<DisplayUnits />);
});

describe("display units add loads without errors", () => {
    it("width,height and generate structure works without errors", () => {
        
    });

    it("add new shelves works without errors", () => {
        
    });

    it("edit shelves works without errors", () => {
        
    });

    it("remove shelves works without errors", () => {
        
    });

    it("save display unit without errors", () => {
        
    });
});